export interface ItemDto {
  kd_barang: string;
  nama_barang: string;
}

export interface TableItemDto {
  id: string;
  kd_barang: string;
  nama_barang: string;
  qty: string;
  subtotal: string;
}
